export interface SliderConfig {
  slidesToShow: number;
  slidesToScroll: number;
  dots: boolean;
  infinite: boolean;
  autoplay: boolean;
  autoplaySpeed: number;
  variableWidth: boolean;
  centerMode: boolean;
}
